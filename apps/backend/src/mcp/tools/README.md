# MCP Tools Directory

This folder contains all MCP tool definitions for the LangChain agent. Each tool is a TypeScript function that wraps business logic and exposes it to the agent.

- `productTools.ts`: Product search and listing
- `cartTools.ts`: Cart operations and CSV export
- `notificationTools.ts`: Email notifications
- `index.ts`: Barrel file to export all tools
